name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
          login-server: demosrgregistry.azurecr.io
          username: demosrgregistry
          password: e4Uw1n9Vf9UshvSEoPhsb2z3ajD5hlNulaxPweoN0x+ACRCvMGLr
      - run: |
          docker build . -t ${{ secrets.ACR_ENDPOINT }}/demosrgregistry:${{ github.sha }}
          docker push ${{ secrets.ACR_ENDPOINT }}/demosrgregistry:${{ github.sha }}